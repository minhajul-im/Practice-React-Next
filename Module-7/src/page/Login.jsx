import Layout from "../layout/Layout";

const Login = () => {
  return (
    <div>
      <Layout>
        <h1 style={{ margin: "0 auto" }}>
          Sorry, I hadn't enough time, that's why I can't do this.
        </h1>
      </Layout>
    </div>
  );
};

export default Login;
