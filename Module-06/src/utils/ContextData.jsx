import { createContext } from "react";

const ContextData = createContext({
  username: "Default_Context_Data: Minhajul Islam (Minhaj)",
  food: "burger",
  task: "task",
});

export default ContextData;
